_format_version: "3.0"

services:
  - name: echo-service
    enabled: true
    url: http://echo
    routes:
      - name: echo-test-route
        paths:
          - /test
      - name: echo-basic-auth-route
        paths:
          - /basic
      # - name: echo-test-route
      #   expression: http.host=="proxy.li.lan" && http.path=^"/efg"
      # - name: echo-basic-auth-route
      #   expression: http.host=="proxy.li.lan" && http.path=="/echo/basic"
      # - name: echo-key-auth-route
      #   expression: http.host=="proxy.li.lan" && http.path=="/echo/key"
      # - name: echo-jwt-auth-route
      #   expression: http.host=="proxy.li.lan" && http.path=="/echo/jwt"